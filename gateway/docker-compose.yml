services:
  gateway:
    build: .
    image: eventhub/gateway
    container_name: eventhub-gateway
    ports:
      - "8082:8082"
    networks:
      - microservices-network

networks:
  microservices-network:
    external: true
